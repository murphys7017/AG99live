# V2 当前实现状态与下一步

快照日期：2026-04-21

## 代码基线

- `d7e761a`：V2 协议迁移（主链路切换为 `type=大类.子类`）
- `1cafe2d`：Live2D 扫描基础能力与前端渲染底座
- `72a6852`：基础动作库“宽进严出”筛选流程（规则 + LLM + fallback）
- `cb2a9cc`：Action Lab、`engine.motion_plan` 预览桥接、右键菜单位置修复

## 模块状态

### Adapter（AstrBot 插件）

- 状态：可运行
- 已完成：
  - `WebSocket` 双向实时消息
  - `HTTP` 静态资源分发
  - V2 协议入站校验与分发
  - 文本/语音/图片主链路
  - 历史记录、心跳、背景列表等 `system.*` 消息
  - Live2D 扫描 + 基础动作库产出
  - 动作库 LLM 严格筛选（可配置、可回退、带缓存签名）
- 当前限制：
  - `engine.motion_plan` 仅测试占位，不执行真实动作

### Frontend（Electron + Vue）

- 状态：可运行
- 已完成：
  - 桌宠窗口（pet 模式）
  - 设置窗口 / 历史窗口 / Action Lab 窗口
  - 单地址配置（用户只填一个地址，内部派生 WS/HTTP）
  - 与适配器 V2 协议对接（文本、语音、控制、系统同步）
  - 发送文本时可选附带实时桌面截图
  - 基础动作库可视化筛选 + 预览计划生成 + 发送测试消息
- 当前限制：
  - Live2D 真实动作引擎未接入（当前仅可做消息级预览测试）

## 协议状态

### `input.*`

- 已用：`input.text`、`input.audio_stream_*`、`input.mic_audio_data`、`input.raw_audio_data`、`input.mic_audio_end`

### `output.*`

- 已用：`output.text`、`output.audio`、`output.image`、`output.transcription`

### `control.*`

- 已用：`control.turn_started`、`control.turn_finished`、`control.interrupt`、`control.playback_finished`、`control.start_mic`、`control.synth_finished`、`control.error`

### `system.*`

- 已用：`system.server_info`、`system.model_sync`、历史相关消息、背景列表、心跳

### `engine.*`

- 已定义：`engine.motion_plan`、`engine.expression_plan`、`engine.idle_state`、`engine.look_target`
- 已接线：`engine.motion_plan` 入站允许 + 前端发送预览
- 未完成：真实动作规划和执行

## 已验证

- `adapter`：`pytest -q`，8 passed
- `frontend`：`npm run typecheck` 通过

## 下一步（建议顺序）

1. 接入前端真实动作执行器（先支持参数写入与最小时间轴）
2. 将 `engine.motion_plan` 从“测试消息”升级为“可执行计划”
3. 打通动作引擎回执（开始/完成/失败）并补齐诊断信息
4. 再决定哪些旧兼容消息可裁剪，收敛协议负担
