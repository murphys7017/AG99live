# 冻结的远程执行器迁移说明

> 状态：冻结。本文不是 AG99live 的当前设计或实现指南。

Remote Operator 的代码和配置暂时保留在本仓库，供后续迁移到其他项目时参考。它不属于 AG99live 的当前产品能力、一期完成范围或二期路线；本仓库不再为它新增功能、修复行为或维护运行文档。

迁移时应重新审视其安全模型、执行器协议、权限与审批策略，以及与 AstrBot 交互生命周期的耦合关系。不得将此处保留的旧实现视为可直接复用的生产方案。

与当前 AG99live 有关的边界只有两点：

- 正式对话、动作、TTS 和播放链路不得依赖 Remote Operator；它们以 `output.segment.v5`、`control.synth_finished` 和前端 PlaybackTimeline 为唯一主路径。
- 如果冻结代码仍被显式启用，其失败、结果或配置不得影响常规互动身体链路；迁移前不再扩展该行为。
