# 前端系统模块架构说明

本文描述 AG99live 前端当前的模块边界、主数据流和后续收口方向。

这里的“模块”不是按文件夹机械划分，而是按系统职责划分。当前代码已经基本形成这些模块，但部分边界仍处在渐进收口阶段。

## 1. 总体判断

当前前端是一个桌宠运行时系统，不只是普通 UI。

它的核心链路是：

```text
Adapter Protocol 接收后端消息
-> Turn Playback Pipeline 编排文本 / 音频 / 动作
-> Motion Engine 编译或调度动作
-> Avatar Runtime 驱动 Live2D
-> Runtime Bridge 同步状态给多窗口工具
```

当前结构方向是合理的：

- 主宠物窗口是运行时 owner。
- 辅助窗口通过 command / snapshot 与主窗口通信。
- 文本、音频、动作已经统一写入 `TurnPlaybackSession`。
- 起播编排和完成判定已经分离。
- 动作引擎不直接处理 WebSocket 协议。

当前仍需继续收口的地方：

- `useAdapterConnection.ts` 仍然承载过多职责。
- Pipeline 对 Adapter 的依赖接口还不够窄。
- Runtime snapshot publisher 直接读取较多模块内部状态。
- 前后端协议契约还没有单一生成源。

## 2. 模块总览

当前建议把前端理解为 8 个一级模块，外加 2 个横切层。

```text
Desktop Host
Runtime Bridge
Adapter Protocol
Turn Playback Pipeline
Model Capability
Motion Engine
Avatar Runtime
Authoring Tools

Audio IO
Contracts
```

整体关系：

```text
Desktop Host
  -> Runtime Bridge
      -> Pet Runtime Controller
          -> Adapter Protocol
          -> Turn Playback Pipeline
          -> Model Capability
          -> Motion Engine
          -> Avatar Runtime
          -> Authoring Tools projection
```

更贴近运行时的数据方向：

```text
Adapter Protocol
  -> Turn Playback Pipeline
      -> Motion Engine
          -> Avatar Runtime

Model Capability
  -> Motion Engine
  -> Authoring Tools

Pet Runtime Controller
  -> Runtime Bridge
      -> Authoring Tools
```

## 3. Desktop Host

职责：提供 Electron 桌面宿主能力，不参与业务协议和播放语义。

主要位置：

- `frontend/electron/main/index.ts`
- `frontend/electron/main/window-manager.ts`
- `frontend/electron/main/menu-manager.ts`
- `frontend/electron/preload/index.ts`

当前负责：

- 创建宠物窗口、overlay、设置、历史、Action Lab、Profile Editor。
- 管理透明窗口、鼠标穿透、窗口拖动。
- 提供右键菜单。
- 提供桌面截图。
- 暴露 preload API：`window.ag99desktop`。

边界要求：

- 不理解 Adapter V2 协议。
- 不理解 `TurnPlaybackSession`。
- 不直接参与动作编译和 Live2D 参数计划。

## 4. Runtime Bridge

职责：负责多窗口之间的 snapshot 同步和 command 转发。

主要位置：

- `frontend/src/composables/useDesktopBridge.ts`
- `frontend/src/composables/usePetRuntimeSnapshotPublisher.ts`
- `frontend/src/types/desktop.ts`
- `frontend/src/desktop-bridge/snapshot.ts`

当前负责：

- 主宠物窗口发布 `DesktopRuntimeSnapshot`。
- 主宠物窗口发布 `DesktopModelProjectionSnapshot`，供工具窗口读取模型能力投影。
- 辅助窗口发送 `DesktopRuntimeCommand`。
- Profile Editor 使用独立的 profile authoring command / snapshot。
- 使用 `BroadcastChannel` 做实时同步。
- 使用 `localStorage` 做部分 snapshot 的窗口间兜底和初始恢复。

当前 snapshot ownership：

| Snapshot | Owner | 是否持久化 | 用途 |
| --- | --- | --- | --- |
| `DesktopRuntimeSnapshot` | Pet 主窗口 | 是 | 运行时状态、连接状态、当前播放摘要 |
| `DesktopModelProjectionSnapshot` | Pet 主窗口 | 否，当前为 runtime-only projection | 当前模型、profile、动作库等工具只读投影 |
| `DesktopProfileAuthoringSnapshot` | Profile Editor / Pet 主窗口协作 | 是 | Profile 编辑草稿、保存状态和编辑工具态 |
| motion tuning samples projection | Pet 主窗口 | 否，正式样本事实在后端 | 动作调参窗口读取的样本投影和诊断 |

当前 owner 关系：

```text
Pet 主窗口：运行时 owner
辅助窗口：工具窗口，只发 command / 读 snapshot
```

边界要求：

- 辅助窗口不直接持有 Adapter WebSocket。
- 辅助窗口不直接修改 Turn Pipeline 内部状态。
- 主窗口负责解释 command 并调用对应运行时模块。

当前结构风险：

- `usePetRuntimeSnapshotPublisher.ts` 直接读取大量模块内部字段，耦合面较宽。

后续收口方向：

- 让各模块提供自己的 snapshot projection，再由 publisher 合并发布。

## 5. Pet Runtime Controller

职责：主宠物窗口的组合根，负责装配模块和转发命令。

主要位置：

- `frontend/src/composables/usePetDesktopController.ts`
- `frontend/src/views/PetDesktopView.vue`

当前负责创建并连接：

- `useAdapterConnection`
- `useModelSync`
- `useTurnPlaybackSessionStore`
- `useTurnPlaybackOrchestrator`
- `usePlaybackCompletionCoordinator`
- `useModelEngine`
- `usePreviewMotionPlayer`
- `useDesktopBridge`
- `usePetRuntimeSnapshotPublisher`

当前还负责：

- 处理辅助窗口 command。
- 处理 Profile Editor command。
- 计算 `connectionState`、`connectionLabel`、`stageMessage`、`aiState`。
- 应用 ambient motion 设置。
- 触发自动连接 Adapter。

边界要求：

- 它可以装配模块，但不应发明新的播放语义。
- 它可以转发命令，但不应把具体协议处理写在自己内部。

当前结构判断：

- 作为 composition root 是合理的。
- 业务逻辑略多，但目前可接受。

后续收口方向：

- 如继续膨胀，可拆出 `useDesktopRuntimeCommandHandler`。

## 6. Adapter Protocol

职责：连接 AstrBot Adapter，处理 V2 WebSocket 协议收发和边界校验。

主要位置：

- `frontend/src/composables/useAdapterConnection.ts`
- `frontend/src/adapter-connection/envelope.ts`
- `frontend/src/adapter-connection/inboundProtocol.ts`
- `frontend/src/adapter-connection/inboundEvents.ts`
- `frontend/src/adapter-connection/inboundPayloads.ts`
- `frontend/src/adapter-connection/outboundActions.ts`
- `frontend/src/adapter-connection/protocolMessageTypes.ts`
- `frontend/src/adapter-connection/modelSyncRewrite.ts`
- `frontend/src/adapter-connection/address.ts`
- `frontend/src/composables/useAdapterHistory.ts`
- `frontend/src/composables/useAdapterMotionTuning.ts`

当前入站流程：

```text
WebSocket raw message
-> parseInboundEnvelope
-> mapInboundEnvelopeToEvent
-> dispatchInboundEvent
-> session / model / audio / history / motion tuning 状态写入
```

当前出站流程：

```text
runtime action
-> buildMessageEnvelope
-> WebSocket send
```

常见出站协议：

- `input.text`
- `control.interrupt`
- `control.playback_finished`
- `system.history_*`
- `system.semantic_axis_profile_save`
- `system.motion_tuning_sample_*`
- `engine.motion_intent` / `engine.motion_plan` preview

边界要求：

- 协议 envelope 和 payload 校验应尽量在这一层完成。
- 进入运行时的事件应尽量是已经定型的内部事件。
- Adapter 层不应该直接包含动作编译逻辑。

当前结构风险：

- `useAdapterConnection.ts` 仍然混合了 transport、dispatcher、runtime facade、audio bridge、history、motion tuning、profile save 等职责。
- 部分 message type 字符串仍散落在模块中。

后续收口方向：

```text
AdapterConnectionTransport
AdapterProtocolDispatcher
AdapterOutboundClient
AdapterAudioBridge
AdapterRuntimeFacade
Feature clients: history / motion tuning / profile save
```

## 7. Turn Playback Pipeline

职责：组织一个 user input 对应 turn 内的多个 assistant output segment，并决定每个 segment 的文本、音频、动作起播和整轮完成回执。

主要位置：

- `frontend/src/turn-playback/session.ts`
- `frontend/src/turn-playback/selectors.ts`
- `frontend/src/composables/useTurnPlaybackSessionStore.ts`
- `frontend/src/composables/useTurnPlaybackOrchestrator.ts`
- `frontend/src/composables/turnPlaybackOrchestratorCore.ts`
- `frontend/src/composables/usePlaybackCompletionCoordinator.ts`

核心对象：

```text
TurnPlaybackSession
  -> backend / phase / interrupted
  -> segments: Map<messageId, TurnPlaybackSegment>
       -> text
       -> audio
       -> motion
```

建模原则：

```text
一个 user input
-> 一个 TurnPlaybackSession
-> 多个 TurnPlaybackSegment
-> 每个 segment = text + audio + motion
```

核心 ID：

- `orchestration_id`：优先的 session 聚合 ID。
- `turn_id`：后端 turn 生命周期 ID，缺少 orchestration 时作为 fallback。
- `message_id`：segment ID，用于绑定 text / audio / motion。

当前 session key 解析顺序：

```text
orch:<orchestration_id>
-> turn:<turn_id>
-> ephemeral:<serial>
```

ID 契约：

| ID | 归属层级 | 作用 | 缺失时策略 |
| --- | --- | --- | --- |
| `orchestration_id` | 播放编排层 | 优先 session 聚合 ID | 缺失时退到 `turn_id` |
| `turn_id` | 后端 turn 生命周期 | 连接后端 turn_started / turn_finished / interrupt | 缺失时只能依赖当前 active session 或生成 ephemeral session |
| `message_id` | 单条回复 segment | 绑定同一片段的 text / audio / motion | 新协议路径必须携带；mapper 仅保留 legacy fallback 作为异常兜底 |

当一轮先以 `turn_id` 创建 session，后续补到 `orchestration_id` 时，session store 负责把 `turn:<turn_id>` 提升为 `orch:<orchestration_id>`，避免同一轮被拆成两个 session。

### 7.1 Session Store

职责：保存事实状态。

主要负责记录：

- segment.text received / released / delivered
- segment.audio received / started / terminal
- segment.motion received / released / started / completed / absent
- backend turnStarted / turnFinished / success / reason
- session phase
- active session

### 7.2 Release Orchestrator

职责：决定什么时候释放文本、音频和动作。

它只管开始，不管结束。

核心等待窗口：

- `AUDIO_MOTION_SYNC_WAIT_MS = 820`
- `TEXT_ONLY_RELEASE_WAIT_MS = 260`

典型策略：

- text/audio/motion 都 ready：一起释放。
- text/audio ready 但 motion 未到：短等 motion。
- 只有 text ready：短等 audio。
- audio absent：按无音频路径释放。
- late motion：后续仍可交给 Motion Engine 补播。

### 7.3 Completion Coordinator

职责：判断后端输出队列是否关闭、本地播放是否全部结束，并回传 `control.playback_finished`。

它只管结束，不管开始。

本地 settled 条件：

- text delivered
- audio 进入 `completed` / `failed` / `absent`
- motion `completed` 或明确 `absent`

当前实现中的协议收口条件：

```text
synth_finished received
and all segments locally settled
=> send control.playback_finished

then backend turn_finished received
=> session completed
```

信号职责：

- `control.synth_finished`：后端输出队列关闭，不会再追加新的 segment。
- `control.playback_finished`：前端已经播放完该 turn 内所有 segment。
- `control.turn_finished`：后端收到 playback_finished 后确认整轮闭环收口。

注意：`control.synth_finished` 不要求早于所有播放完成。前端可以边收边播，直到队列关闭且所有 segment settled 后再发送 `playback_finished`。

边界要求：

- Pipeline 不应该理解 WebSocket 原始消息。
- Pipeline 不应该编译动作。
- Pipeline 只应该通过窄接口通知 Adapter 发送 `playback_finished`。

当前结构风险：

- `useTurnPlaybackOrchestrator` 和 `usePlaybackCompletionCoordinator` 仍依赖完整 adapter 返回对象，而不是窄接口。

后续收口方向：

- 抽出 `PlaybackReleasePort` 和 `PlaybackAckPort`。

建议端口形态：

| Port | 调用方 | 提供方 | 作用 |
| --- | --- | --- | --- |
| `PlaybackReleasePort` | Turn Playback Pipeline | Adapter / Audio IO / ModelEngine facade | release text/audio/motion |
| `PlaybackAckPort` | Completion Coordinator | Adapter outbound client | 发送 `control.playback_finished` |
| `MotionPlaybackPort` | Motion Engine | Avatar Runtime | 执行参数计划 |
| `RuntimeSnapshotPort` | 各 runtime 模块 | Snapshot publisher | 提供模块级 projection |

## 8. Model Capability

职责：维护后端同步来的模型能力事实。

主要位置：

- `frontend/src/composables/useModelSync.ts`
- `frontend/src/types/protocol.ts`
- `frontend/src/action-lab/parameterActionPreview.ts`

当前承载的数据：

- 当前 session / conf 信息。
- 当前选中模型。
- 可用模型列表。
- resource scan。
- parameter scan。
- expression scan。
- motion resource pool。
- base action library。
- parameter action library。
- semantic axis profile。
- calibration profile。
- runtime cache errors。

边界要求：

- 它是模型能力事实来源。
- Motion Engine 和 Authoring Tools 消费它。
- 它不负责播放编排。

当前结构判断：

- 当前 `useModelSync` 作为轻量 store 可用。
- 随着模型能力继续扩大，后续可以拆出 normalizer、selectors 和 model selection service。

## 9. Motion Engine

职责：把动作 payload 变成可执行的 Live2D 参数计划，或调度已有参数计划播放。

主要位置：

- `frontend/src/model-engine/useModelEngine.ts`
- `frontend/src/model-engine/contracts.ts`
- `frontend/src/model-engine/normalize.ts`
- `frontend/src/model-engine/planParser.ts`
- `frontend/src/model-engine/compiler.ts`
- `frontend/src/model-engine/settings.ts`
- `frontend/src/composables/usePreviewMotionPlayer.ts`

当前输入：

- `engine.motion_intent.v2`
- `engine.parameter_plan.v2`
- 当前 `ModelSummary`
- 当前 `SemanticAxisProfile`
- 当前 motion engine settings
- 当前音频播放上下文

当前输出：

- 编译后的 `engine.parameter_plan.v2`
- `motionPlayer.playPlan(...)`
- motion playback record

边界要求：

- 不直接处理 WebSocket。
- 不直接决定文本和音频何时释放。
- 可读取必要的 session/audio timing 信息，但不应成为 Turn Pipeline 的事实来源。

当前结构判断：

- 通过 dependency injection 接入外部能力，方向合理。
- 仍理解一部分 session segment 结构，属于轻微耦合。

## 10. Avatar Runtime

职责：加载和渲染 Live2D 模型，并执行参数计划。

主要位置：

- `frontend/src/components/DesktopPetCanvas.vue`
- `frontend/src/composables/useLive2dRenderer.ts`
- `frontend/src/composables/usePreviewMotionPlayer.ts`
- `frontend/src/live2d/WebSDK/**`

当前负责：

- 初始化 Cubism SDK。
- 加载模型和纹理。
- 渲染透明桌宠 canvas。
- 执行动作参数计划。
- 支持 soft handoff。
- 处理 ambient / idle motion。

边界要求：

- 不理解后端协议。
- 不理解 Turn Playback Session。
- 只接受模型加载和参数计划执行请求。

## 11. Authoring Tools

职责：提供配置、历史、动作实验室、Profile 编辑、动作调参等工具窗口。

主要位置：

- `frontend/src/views/SettingsWindowView.vue`
- `frontend/src/views/HistoryWindowView.vue`
- `frontend/src/views/ActionLabWindowView.vue`
- `frontend/src/views/ProfileEditorWindowView.vue`
- `frontend/src/components/MotionTuningPanel.vue`
- `frontend/src/components/SemanticAxisProfileEditor.vue`
- `frontend/src/components/BaseActionPreviewPanel.vue`

当前交互模式：

```text
工具窗口
-> useDesktopBridge.sendCommand(...)
-> Pet Runtime Controller
-> 对应 runtime 模块
-> snapshot 回传工具窗口
```

边界要求：

- 工具窗口不直接连接 Adapter。
- 工具窗口不直接写主运行时状态。
- 工具窗口通过 command 表达意图，通过 snapshot 展示结果。

## 12. Audio IO 横切层

职责：处理音频播放和麦克风输入。

主要位置：

- `frontend/src/adapter-connection/audioPlayback.ts`
- `frontend/src/adapter-connection/audioPlaybackActions.ts`
- `frontend/src/adapter-connection/microphoneCapture.ts`
- `frontend/src/composables/useAdapterConnection.ts`

输出音频方向：

```text
output.audio
-> queue audio
-> release audio
-> start local playback
-> markAudioStarted / markAudioTerminal
-> Completion Coordinator
```

输入麦克风方向：

```text
microphone capture
-> input.raw_audio_data
-> input.mic_audio_end
```

当前结构风险：

- 音频事实状态同时存在于 adapter state 和 session segment 中。

后续收口方向：

- 明确 `segment.audio` 是事实来源，adapter audio fields 作为兼容投影；或反过来明确 adapter 是事实来源。当前更推荐前者。

## 13. Contracts 横切层

职责：定义前后端协议和窗口间通信契约。

主要位置：

- `frontend/src/types/protocol.ts`
- `frontend/src/types/desktop.ts`
- `frontend/src/types/semantic-axis-profile.ts`
- `frontend/src/adapter-connection/protocolMessageTypes.ts`
- `frontend/src/adapter-connection/inboundPayloads.ts`

当前包括：

- Adapter V2 envelope。
- Adapter message types。
- Motion schemas。
- Model sync payload。
- Desktop runtime command / snapshot。
- Profile authoring command / snapshot。
- Semantic axis profile。

当前结构风险：

- 前后端协议没有单一生成源。
- 前端仍存在少量硬编码 message type。
- 大型 payload 类型仍主要靠手写维护。

后续收口方向：

- 短期继续集中前端 message type。
- 中期考虑协议清单或 JSON Schema 生成 Python / TypeScript 契约。

## 14. 关键主流程

### 14.1 文本对话主流程

```text
辅助窗口输入文本
-> DesktopRuntimeCommand: send_text
-> usePetDesktopController
-> adapter.sendText
-> input.text
-> 后端
-> control.turn_started
-> output.text / output.audio / engine.motion_*
-> useAdapterConnection dispatch
-> TurnPlaybackSessionStore
-> useTurnPlaybackOrchestrator release
-> text display / audio playback / motion engine
-> control.synth_finished closes segment queue
-> all segments locally settled
-> control.playback_finished
-> control.turn_finished
-> session completed
```

### 14.2 模型同步主流程

```text
后端扫描模型
-> system.model_sync
-> useAdapterConnection
-> useModelSync
-> selectedModel / selectedSemanticAxisProfile
-> Motion Engine / Authoring Tools / Runtime Snapshot
```

### 14.3 动作测试主流程

```text
Action Lab / Profile Editor
-> preview_motion_payload
-> usePetDesktopController
-> modelEngine.playPreviewPayload 本地播放
-> adapter.sendMotionPayloadPreview 发给后端记录或验证
```

## 15. 推荐依赖方向

建议后续维护时坚持以下方向：

```text
Authoring Tools -> Runtime Bridge -> Pet Runtime Controller

Pet Runtime Controller -> Adapter Protocol
Pet Runtime Controller -> Turn Playback Pipeline
Pet Runtime Controller -> Model Capability
Pet Runtime Controller -> Motion Engine
Pet Runtime Controller -> Avatar Runtime

Adapter Protocol -> Turn Playback Pipeline
Turn Playback Pipeline -> Motion Engine
Motion Engine -> Avatar Runtime
Model Capability -> Motion Engine
Model Capability -> Authoring Tools
Turn Playback Pipeline -> Adapter Protocol(playback_finished only, via narrow port)
```

不建议出现：

- Avatar Runtime 直接访问 Adapter Protocol。
- Authoring Tools 直接修改 Turn Playback Session。
- Motion Engine 直接发送 WebSocket 消息。
- Adapter Protocol 内部编译动作计划。
- Desktop Host 理解业务协议。

## 16. 当前结构评分

按当前代码状态评估：

```text
模块划分思路：8/10
当前代码边界：6.5/10
可维护性：7/10
演进潜力：8/10
短期稳定性：7.5/10
```

总体结论：

```text
结构方向正确，核心模块已经成形；当前仍处于“模块概念清楚，但部分代码边界尚未完全硬化”的阶段。
```

## 17. 后续收口优先级

建议优先级：

1. 后端 `synth_finished` 发送点收口到 turn 输出队列关闭语义。
2. 为 Turn Pipeline 注入窄接口，而不是依赖完整 adapter。
3. 把 `useAdapterConnection` 逐步拆为 transport、dispatcher、runtime facade、feature clients。
4. 明确 `segment.audio` 是音频事实来源，adapter audio fields 只做投影。
5. 继续把前端剩余 message type 硬编码收敛到 `protocolMessageTypes.ts`。
6. 后端 `parse_inbound_message` 严格校验协议版本。
7. 补充 `orchestration_id` / `turn_id` / `message_id` 三层 ID 契约文档。
8. 为 snapshot publisher 引入模块级 projection。

## 18. 相关文档

- [当前前后端动作链路结构说明](./当前前后端动作链路结构说明.md)
- [文本语音动作同步播放编排设计](./文本语音动作同步播放编排设计.md)
- [项目结构优化路线图](./项目结构优化路线图.md)
- [后端主导数据边界与执行计划](./后端主导数据边界与执行计划.md)
