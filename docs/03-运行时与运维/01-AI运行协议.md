# AI 代理运行协议

使用 `AGENTS.md` 和 `.ai/index.md`。

循环：

1. 确定目标、成功标准和相关时的非目标。
2. 分类任务并评估风险。
3. 检查相关文件。
4. 仅在风险或复杂度需要时进行规划。
5. 实施最小的正确变更。
6. 审阅、校验、纠正并报告缺口。

状态：

- 仅在范围、风险、架构影响、假设或校验状态应显式时使用 `.ai/state.yaml`。
- 在记录持久化架构决策或项目风险前询问。

完成：

- 目标已满足或显式缺口。
- 已知校验状态。
- 可见剩余风险。
- 无隐藏的所需纠正。
