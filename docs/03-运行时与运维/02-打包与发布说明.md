# 打包与发布说明

发布包内容：

- `AGENTS.md`
- `.ai/index.md`
- `.ai/state.yaml`
- `docs/runtime/release_package.md`
- `tools/validate_runtime.py`

安装方法：解压到目标仓库根目录，然后运行：

```bash
python tools/validate_runtime.py
```

如需，为目标仓库自定义 `AGENTS.md`。
