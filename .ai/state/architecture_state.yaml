architecture:
  stability: stable
  pending_breaking_change: false
  boundary_changes:
    - completed: extract inline motion parsing/validation from TurnCoordinator to motion/inline_motion.py
    - completed: TurnCoordinator reduced from ~1237 to ~1026 lines
    - completed: dataflow review of extraction confirms all 4 paths equivalent
    - completed: governance compliance audit confirms no violations
  notes:
    - Backend motion/ module now contains inline_motion.py (inline animation parsing, schema validation, contract building)
    - TurnCoordinator retains core turn lifecycle (handle_msg, emit_message_chain, close_turn_output_queue, finalize_turn)
    - All extracted functions are pure functions with no class state dependency
    - 146 tests pass, no regression
