task:
  class: refactor
  risk: medium
  phase: completed
  scope: frontend adapter-connection structure
context:
  confidence: high
  assumptions:
    - Adapter external APIs should remain stable while implementation folders are reorganized.
    - Motion engine and action library are out of scope for the current structural pass.
  unresolved_questions: []
architecture:
  stability: stable
  boundary_changes:
    - frontend/src/adapter-connection split into core, inbound, outbound, runtime, and features.
verification:
  checks_run:
    - frontend npm run typecheck
    - frontend npm test
  checks_failed: []
  validation_gap: none known
runtime:
  mode: minimal_v1
