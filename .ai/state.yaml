task:
  class: refactor
  risk: high
  phase: phase_two_a3
  status: implementation_complete
  scope: "Unify existing semantic-group and SpeechPose timing inside the shared PerformanceSchedule, record event-to-parameter-node provenance, and preserve public protocols plus presentation behavior."

context:
  confidence: high
  assumptions:
    - Product phase one implementation is complete.
    - Phase two optimizes complete character performance rather than treating gaze, face, Physics, Prompt, or motion range as independent product directions.
    - Performance Director is a ModelEngine compile-time orchestration responsibility, not a second player, Timeline, parameter mixer, or backend motion service.
    - PerformanceSchedule now owns phrase, semantic-step, semantic-group and SpeechPose timing events plus event-to-parameter-node diagnostics.
    - Phase A introduces one internal PerformanceSchedule while preserving motion intent v4, parameter plan v3, PlaybackTimeline, ParameterPresentation, ActiveParameterMixer, SpeechSignalRuntime, and Cubism Physics boundaries.
    - Phase A first records estimated phrase-to-sequence alignment; motion intent v4 is not extended unless real runtime samples prove internal alignment insufficient.
    - Gaze, face, hesitation, release, and residual are Schedule-consuming body-part strategies, not independent clocks or parallel dynamics systems.
    - Physics setting diagnostics remain a separate model-level tuning phase and do not become Performance Director or ParameterMixer control logic.
    - Manual observation, model tuning, and live runtime acceptance are not implementation backlog.
    - The first parameter track graph is a compile-time ModelEngine structure, not a second runtime graph engine or player.
    - The graph emits the existing engine.parameter_plan.v3 keyframes and modulation fields; no protocol migration is required.
    - ActiveParameterMixer remains a per-frame contribution mixer and does not acquire semantic timing ownership.
    - Deeper dynamics and Physics work, Motion Lab editing, small-model training, VTS semantic export, remote-operator separation, cross-host deployment, and release packaging belong to phase two or later.
    - Obsolete documents, code, tests, protocol versions, and migration history are deleted rather than retained as compatibility guidance.
    - Public media, disposable caches, and durable Motion Lab or Motion Tuning data must use separate directories and failure domains.
    - PlaybackTimeline remains the authoritative frontend execution lifecycle; SessionStore stores its settled projection and AdapterConnection does not own a parallel audio terminal state machine.
    - Cubism native motion, expression, drag, blink, and breath remain the base layer; ActiveParameterMixer owns AG99 active overlays before Cubism Physics.

architecture:
  phase_boundary:
    source: docs/01-架构与结构/06-一期完成边界与二期入口.md
    phase_one: complete
    phase_two: planned
  product_flow:
    - AstrBot owns conversation, event queues, main-model calls, and TTS.
    - The Adapter projects Live2D capabilities, validates motion intent, binds identities, and emits one atomic output.segment.v3.
    - SessionStore owns settled protocol facts; PlaybackTimeline owns the segment clock and required sink lifecycle.
    - ModelEngine compiles engine.motion_intent.v4 into CompiledSemanticMotion and engine.parameter_plan.v3.
    - ParameterPresentation and ActiveParameterMixer produce one active parameter frame before Cubism Physics.
    - Live2D WebSDK owns final parameter writes, lip sync, Physics, model update, and rendering.
  identity:
    turn_id: Conversation turn identity shared across AstrBot, Adapter, frontend, and Motion Lab.
    message_id: Logical assistant output segment identity.
    tts_request_id: One TTS generation lifecycle.
    run_id: One accepted motion execution owned by one segment.
  protocols:
    source: astrbot_plugin_ag99live_adapter/protocol/schema_manifest.json
    websocket_envelope: v2
    output_segment: output.segment.v3
    motion_intent: engine.motion_intent.v4
    parameter_plan: engine.parameter_plan.v3
    semantic_axis_profile: ag99.semantic_axis_profile.v3
    relation_graph: ag99.semantic_axis_relation_graph.v1
    voice_following_profile: ag99.voice_following_profile.v3
    model_info: live2d_scan.v3
  ownership_rules:
    - The main model is the only source of semantic motion direction and level.
    - Optional performance curves never block TTS, segment release, or base motion playback.
    - ActiveParameterMixer combines resolved active contributions and does not interpret semantic levels or own Timeline state.
    - Cubism Physics output parameters are never directly driven by semantic motion or active-parameter smoothing.
    - Invalid identity, schema, timing, binding, or lifecycle state fails visibly instead of activating a fallback path.

verification:
  policy:
    - Use focused source inspection, documentation link checks, protocol-manifest validation, Mermaid source checks, and git diff review for documentation work.
    - Automated tests are low-confidence boundary smoke checks and are not end-to-end evidence.
    - Live Electron, AstrBot, TTS, and Live2D observation is user-led acceptance, not unfinished implementation work.
  current_checks:
    - Phase A1 now creates one internal PerformanceSchedule for each complete pose or sequence compilation and shares it with every pose compile context.
    - Canonical phrase segmentation, boundary classification, weighting, emphasis, and normalized phrase windows were removed from SpeechPoseStage and moved into the shared Schedule compiler.
    - Phase A2 now validates semantic sequence shape, computes duration-weighted step windows, and estimates phrase/step overlap inside the same PerformanceSchedule.
    - Parameter Track Graph consumes Schedule step starts; its duplicate duration-weight calculation and local step-start algorithm were removed.
    - Schedule failures are explicit; no equal-weight, legacy, or silent-repair branch was added.
    - Duration-only schedules preserve the placeholder phrase used by SpeechPose but record no estimated phrase/step alignment.
    - Schedule duration remains the audio/semantic body duration; Track Graph transition and residual tail extension does not stretch phrase or step windows.
    - A3 moved semantic-group offset, transition, hold, residual, SpeechPose intro, phrase onset, transition and release into PerformanceSchedule while retaining the existing deterministic seed formulas and timing constants.
    - SpeechPose now owns only valid channel selection, preset selection, gesture direction and amplitude; Parameter Track Graph only projects scheduled events as v3 keyframes.
    - Every scheduled semantic keyframe and speech modulation point records its source event through transform trace v2; Motion Lab renders the phrase / step / event / parameter relation.
    - Desktop runtime snapshot rejects any motion history record carrying a non-v2 or structurally incomplete PerformanceSchedule trace; stale development history is discarded instead of reaching Motion Lab.
    - Performance Schedule diagnostics use independent performance_schedule_trace.v2 versioning; semantic_motion_transform.v5 remains unchanged so deterministic nine-level sampling identities do not shift.
    - Renderer TypeScript validation passed after the A3 migration; no live Electron, TTS, or Live2D sequence playback has yet exercised the refactored path.
    - The current AstrBot-to-Adapter-to-Timeline-to-ModelEngine-to-Cubism source path was re-traced for the phase-two performance orchestration review.
    - Superseded action backlogs, implementation histories, ParameterMixer migration notes, duplicate architecture summaries, and the premature small-model target draft were removed.
    - Documentation indexes, public READMEs, target dependency rules, and VTS recorder boundaries were aligned with the retained authority documents.
    - Relative Markdown links, protocol schema manifest, Markdown fences, Mermaid file inventory, deleted-document references, and git diff whitespace were checked.
    - A3 changed only the ModelEngine compile-time scheduling path, transform trace and Motion Lab diagnostics; no live AstrBot, Electron, TTS, VTube Studio, or Live2D execution was exercised.
    - Current source was re-checked: PerformanceSchedule owns phrase, duration-weight step and existing body-part timing events; Physics exposure remains limited to file/count scanning plus a global terminal response scale.

backlog:
  phase_two:
    - Phase A1 complete: internal PerformanceSchedule plus shared phrase analysis.
    - Phase A2 complete: sequence-step scheduling plus observable estimated phrase alignment.
    - Phase A3 complete: shared body-part timing events and Director/Motion Lab source diagnostics.
    - Phase B: gaze, face, hesitation, release, and dynamic residual strategies consuming the shared Schedule.
    - Consider explicit sequence-step phrase identity only after real data proves the internal alignment insufficient.
    - Phase D: Cubism Physics setting diagnostics and model-level presentation profiles, independent from active parameter planning.
    - Motion Lab sequence editing, diversity summaries, and training-data workbench.
    - Small motion-model training and VTube Studio semantic labeling/export.
    - Remote-operator plugin separation, authenticated cross-host deployment, and release packaging.
