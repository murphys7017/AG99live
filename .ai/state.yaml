task:
  class: feature
  risk: medium
  phase: reviewed
  status: complete
  scope: Complete Motion Lab data closure: bind saved samples to canonical turn segments, preserve V4 motion sequences without flattening, and select Prompt references by current-input relevance.

context:
  confidence: high
  assumptions:
    - The user confirmed that the selected Superheroes recording and the Live2D model are cleared for the intended commercial use.
    - The promotional video remains isolated under promo-video and does not become a runtime dependency of AG99live.
    - The final promotional render uses the user-provided cleared soundtrack excerpt and a clean, locally recorded Live2D action capture.
    - AG99live is an active-development desktop Live2D system; obsolete documents, code, and tests are deleted instead of archived in the repository.
    - The main model emits assistant text and one ag99live.motion Persona Effect in the same logical response.
    - The official AstrBot <@anim> transport exists only when Persona Effect injection is unavailable; its inner intent uses v4 and it is not a recovery path for invalid motion.
    - Remote computer control remains because AstrBot may run on a server while the Electron client runs on the user's computer.
    - Performance curve generation is optional and never delays TTS or segment release.
    - Motion Lab samples are trusted only when their user and assistant context is reconstructed from persisted Adapter raw events using turn_id plus message_id.
    - Historical Motion Lab sample records that do not conform to the current V2 sample contract are deleted rather than migrated.

architecture:
  product_flow:
    - User input is forwarded to AstrBot without an AG99-owned busy queue or input rejection gate.
    - AstrBot produces canonical assistant text, TTS generation state, audio material, and Persona Effect data.
    - The adapter aggregates one output.segment.v3 per turn_id plus message_id and sends it atomically.
    - The frontend SessionStore commits the segment once; the orchestrator releases one segment job.
    - PlaybackTimeline owns audio, subtitle, motion, and lip-sync sink lifecycle.
    - ModelEngine compiles semantic motion into a parameter plan or typed resource execution.
    - Live2D WebSDK owns model readiness, per-frame parameter writes, speech energy, lip sync, dynamics, and physics.
  identity:
    turn_id: Conversation turn identity shared across AstrBot, adapter, frontend, and Motion Lab.
    message_id: Logical assistant output segment identity; immediate and final outputs are distinct segments.
    tts_request_id: One TTS generation lifecycle, including retries or multiple audio segments.
    run_id: One accepted motion execution owned by one segment.
  protocols:
    schema_manifest: astrbot_plugin_ag99live_adapter/protocol/schema_manifest.json is the single source for protocol and schema versions; frontend constants are checked against it.
    output: output.segment.v3
    persona_motion: engine.motion_intent.v4
    official_inline_motion: engine.motion_intent.v4 inside the <@anim> transport wrapper
    parameter_plan: engine.parameter_plan.v2
    performance_curve: ag99.performance_curve_hint.v1
    voice_following: ag99.voice_following_profile.v3
    model_info: live2d_scan.v2
    handshake: system.server_info carries the complete schema manifest; the frontend rejects all later messages until every version matches.
  motion_ownership:
    llm: intent_tags, axis_levels from -4 to 4, motion_steps, and optional typed resource intent.
    adapter: Persona Effect registration, strict schema/profile/resource validation, identity binding, and transport.
    model_engine: Level sampling, intensity, coupling, timing, speech gesture planning, relation graph constraints, plan building, and resource policy.
    playback_timeline: Segment-scoped clock and required/optional sink lifecycle.
    live2d_websdk: Exact parameter resolution, per-frame interpolation, lip sync, speech energy, dynamics, and physics.
  settings_ownership:
    model_engine: motionIntensityScale only.
    live2d_presentation: ambientMotionEnabled, physicsResponseScale, and renderDprCap; applied by the renderer before model initialization and atomically through the WebSDK Adapter at runtime.
  playback_rules:
    - Audio-backed subtitle, motion, and lip sync use the matched HTMLAudioElement playing fact.
    - Internal speech-only motion receives the same segment clock reader but does not project started or terminal state into an absent protocol motion slot.
    - Explicit motion-only segments may use a segment-owned synthetic clock.
    - Expected audio failure fails the segment; it never becomes normal no-audio playback.
    - Missing required wiring, invalid payloads, and impossible lifecycle transitions fail visibly.
    - Optional performance curve failure or lateness is recorded as skipped and does not modify the valid base motion.
  data_rules:
    - Motion Lab records raw model output, resolved levels, derived and constrained axes, compiled plans, timing, run identity, and terminal outcome.
    - Runtime facts are persisted asynchronously and do not block the active conversation.
    - Stage diagnostics preserve value sources so final motion remains attributable.
    - Motion Lab sequence samples retain V4 motion_steps and never collapse to a flat pose while being replayed, saved, or used as Prompt references.

verification:
  policy:
    - Automated tests are limited to the smallest public-boundary input/output smoke checks.
    - Tests are not evidence that the end-to-end interaction is correct.
    - Source tracing, structured runtime logs, Motion Lab records, and a live Electron conversation are the primary evidence.
    - Do not run full regression suites by default and do not add mocks to compensate for missing live verification.
  checks_run:
    - Root README protocol names, feature claims, startup paths, documentation links, and image paths were checked against current source and repository files; the two user-selected screenshots were inspected visually.
    - `python scripts/check_protocol_schema_manifest.py` passed.
    - `npm run typecheck:renderer` passed in frontend.
    - `npm run typecheck:websdk` passed in frontend.
    - `npm run typecheck:node` passed in frontend.
    - `python -m compileall -q astrbot_plugin_ag99live_adapter` passed.
    - `check_astrbot_plugin.py astrbot_plugin_ag99live_adapter` reported 0 errors and 0 warnings.
    - git diff --check reports no whitespace errors.
    - `npm run typecheck:renderer` passed for the Motion Lab replay ownership change.
    - `python -m py_compile` passed for the Motion Lab V2 storage, sequence projection, and request-scoped sample selection changes.
    - `npm run typecheck:renderer` passed for the Motion Lab V2 frontend protocol, Action Lab sequence handling, and effective-example projection changes.
    - `python scripts/check_protocol_schema_manifest.py` passed after adding `ag99.motion_tuning_sample.v2`.
    - The basic protocol sample-save smoke test passed, and a direct deterministic selection smoke check chose the matching `开心` sample over an unrelated sample.
    - `npm run typecheck:node` passed for the strict desktop bridge command change.
    - Obsolete frontend preview command and Adapter outbound API references were confirmed absent with `rg`.
    - `npm run typecheck:renderer` and `npm run typecheck:node` passed after assigning stable capture titles to the Live2D and input windows.
    - `npm run typecheck:websdk` passed after replacing the Live2D frame limiter with accumulated frame deadlines.
    - `npm run typecheck:renderer`, `npm run typecheck:websdk`, and `npm run typecheck:node` passed after unifying speech gesture parameter binding.
    - Targeted Python syntax checks, `npm run typecheck:renderer`, `npm run typecheck:websdk`, and a generated SemanticAxisProfile input/output smoke check passed after widening Live2D pose anchors and speech-following dynamics.
    - `npm run typecheck:renderer` and `npm run typecheck:node` passed after the protocol, ModelSync ownership, and documentation review.
    - `npm run typecheck:renderer`, `npm run typecheck:node`, and `npm run test:playback-timeline` passed after separating internal speech-only execution from protocol motion state projection.
    - `python scripts/check_protocol_schema_manifest.py` passed after adding `live2d_scan.v2` to the strict handshake manifest.
    - Targeted `python -m py_compile` passed for every modified Adapter runtime, scanner, protocol, and transport module.
    - Mermaid sources were reviewed against current code; no Mermaid CLI is installed, so diagram rendering was not validated automatically.
    - Python syntax checks passed for the scanner and runtime cache changes.
    - The previous v2 Electron production build passed before the atomic voice-following V3 migration.
    - `npm run build` passed for the atomic V3 migration; the generated renderer contains only `ag99.voice_following_profile.v3`.
    - Mk6 V3 amplitude ratios preserve the previous effective amplitudes within rounding while deriving limits from SemanticAxisProfile.
    - `npm run typecheck` passed in promo-video after integrating the live capture.
    - The 28-second half-resolution preview rendered successfully and its ten-scene contact sheet was visually inspected.
    - The 1920x1080 H.264/AAC final render completed successfully and decoded end-to-end with ffmpeg without errors.
    - A two-turn live Electron conversation was captured from the real terminal, input overlay, Live2D window, and original MiniMax TTS artifacts; the synchronized 14-second dialogue segment was visually inspected.
    - The expanded 44.5-second 1920x1080 H.264/AAC render completed and decoded end-to-end without errors; dialogue peaks at -4.1 dB after a 6 dB intelligibility lift.
  validation_gap:
    - A live AstrBot plus Provider plus TTS plus Electron plus Live2D conversation is required to verify prompt construction, effect delivery, atomic segment release, real audio clock, subtitle, lip sync, motion start, parameter writes, and terminal reporting as one chain.
    - Live AstrBot plus Provider plus TTS plus Electron plus Live2D validation is still required after the clock migration, including a long audio segment and a paused audio segment.
    - Live Electron plus Mk6 validation is required to confirm that physicsResponseScale changes terminal hair, clothing, and accessory response without changing semantic head/body input ranges.
    - Motion Lab must be checked with an explicit neutral axis and a derived non-neutral axis to confirm only the original explicit axis is replayed and saved.
    - Live Electron validation is required to confirm historical sequence replay starts the preserved parameter plan, reports its real terminal status, and rejects previews while conversation motion is active.
    - OBS must be restarted and its Window Capture source refreshed to confirm `AG99live Live2D` and `AG99live Input` replace the previous null window identity.
    - Live Electron observation on the 180 Hz display is required to confirm the renderer remains near its 60 FPS target under the real model workload.
    - A live speech-only segment and a semantic-motion-plus-speech segment are required to confirm voice_following_profile.v3 resolves through the same semantic parameter bindings and remains visually expressive.
    - Live Mk6 observation is required to tune the widened head/body anchors and stronger speech gesture envelope against actual model rigging; static checks cannot establish the desired visual exaggeration.
    - A live speech-only segment with `output.segment.motion=absent` must confirm the plan receives the segment Timeline reader, starts once, and leaves the SessionStore motion terminal at absent.
    - A live Electron + AstrBot conversation must save one V2 pose sample and one V2 sequence sample, then confirm the Adapter reconstructs trusted user/assistant context from raw events and returns the sequence to the next Prompt as `motion_steps`.
    - A second live request with an explicit matching tag or overlapping user text must confirm the Action Lab effective examples and the generated Prompt use the request-scoped sample selection.

backlog:
  - id: motion_lab_sequence_tuning
    status: planned
    summary: Upgrade Motion Lab drafts, persisted samples, and backend few-shot projection to preserve and edit v4 motion_steps without flattening them into one pose.
  - id: motion_lab_training_workbench
    status: planned
    summary: Build a separate UI for querying, replaying, reviewing, and curating persisted Motion Lab records.
  - id: speech_performance_quality
    status: active
    summary: Improve non-periodic speech gestures and Live2D physical expressiveness without adding a parallel playback clock.
  - id: remote_operator_plugin_split
    status: deferred
    summary: Move remote computer execution into a separate AstrBot plugin after the server-side AstrBot architecture is stable.
